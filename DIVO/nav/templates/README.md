# Navigation render assets

Only `nav_bmud_v2_train2_dynamic_pair/` belongs to the current formal protocol.
Files under `legacy_pre_nav_bmud_v2/` were rendered before the fixed-two-Pillar,
parameterized B/M/U/D, and dynamic-Gremlin protocol and must not be used in paper figures.
