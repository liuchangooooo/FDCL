Generate an initial Push-T obstacle generator.

No policy feedback is available yet, so the initial distribution should be easy-to-medium, solvable, and diverse rather than maximally difficult.

Example start pose for reasoning:
- T-block start: x={tx:.3f}, y={ty:.3f}, theta={theta_deg:.0f} deg
- Target pose: `(0, 0, -45 deg)`

Initial generator requirements:
- Use randomized but structured obstacle sampling.
- Preserve feasible space near the T-block start and the target.
- Avoid layouts that completely block all pushing or transport routes.
- Avoid placing obstacles directly on top of the T-block or target.
- Reject unsafe candidates and keep sampling until exactly `num_obstacles` valid obstacles are returned.
- If random sampling fails, use a conservative valid fallback.

Write the complete initial generator function now.
